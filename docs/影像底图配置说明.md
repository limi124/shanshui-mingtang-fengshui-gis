# 影像底图配置说明

前端影像瓦片地址通过 `frontend/.env` 配置：

```env
VITE_IMAGERY_TILE_URL=https://your-tile-service/{z}/{x}/{y}.png
```

不要把地图 Key、Token 或签名写死在代码里。若服务商不允许前端暴露 token，应通过后端代理或 Nginx 安全转发。

第一版矢量底图使用 OpenStreetMap，影像默认使用公开占位服务，地形阴影使用 mock hillshade 图层。
